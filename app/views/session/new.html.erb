<h1>Sign In Create a new Session</h1>

<%= form_tag("/signin", method: "POST") do %>

  <h3>User name: <%= collection_select(:user, :name, User.all, :name, :name) %>
  <h3>Password: <%= password_field_tag :password %>
  <%= submit_tag "Sign In" %>
<% end %>

